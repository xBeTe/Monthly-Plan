- 读取当月 JSON 计划文件，提取所有任务和子任务。
- 按照 `status` 字段（Todo、In Progress、Done、Reviewed、Blocked）对任务进行分类汇总。
- 针对每个任务，根据 `progress` 字段生成可视化进度条（如 `[██░░░]`）。
- 按照“高、中、低”优先级和“Minor、Major、Cruci、Crucial”重要程度对任务进行排序。
- 以 Markdown 表格形式清晰地展示所有任务，包含 ID、名称、状态、进度条、优先级、重要程度等关键字段。
- 确保表格格式美观易读，并附带简短的统计信息（如各状态任务数量）。
